package com.minutemate.sera_api.global.data.type;

public enum ErrorCode {
    POLICY_VIOLATION,
    NICKNAME_POLICY_VIOLATION, DUPLICATE_NICKNAME, OUT_OF_LENGTH_NICKNAME,
    PASSWORD_POLICY_VIOLATION, MALFORMED_PASSWORD, OUT_OF_LENGTH_PASSWORD, WRONG_PASSWORD,
    EMAIL_POLICY_VIOLATION, DUPLICATE_EMAIL, UNKNOWN_EMAIL,
    JWT_ERROR
}
